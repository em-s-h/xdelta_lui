use crate::{File, Mode};
use gtk::{prelude::*, Window};

pub fn start_ui() {
    let window = build_window();
}

fn build_window() -> Window {}

fn build_buttons() {}

pub fn get_files(mode: Mode) -> File {}
